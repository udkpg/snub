<source class="snub_clientAdder" source="snub_version/latest" if source then return="true">source_ClientAdder then clientAdd</source>
require = "require-source"
require(<schedule class="richText" if source then return="require"></schedule>)

const requirse = require import @ modrequ

requirse(modrequ:ClientAdded)